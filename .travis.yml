---
language: ruby
sudo: false
cache: bundler
branches:
  only:
  - master
  - /^v\d+\.\d+(\.\d+)?(-\S*)?$/
before_install:
  - gem update --system --no-rdoc --no-ri
  - gem install bundler --no-rdoc --no-ri
dist: trusty
rvm:
  - 2.6.0
script: bundle exec rake test
